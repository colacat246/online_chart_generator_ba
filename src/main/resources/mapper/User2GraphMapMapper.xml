<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.cv.ocb.mapper.User2GraphMapMapper">
    <select id="getGraphsByUserId" resultType="User2GraphMap">
        select * from user2graph_map where user_id = #{user_id}
    </select>
</mapper>